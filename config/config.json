{
	"dim" : 200,				// Lattice size in one dimension
	"n_neigh" : 6,				// Lattice coordination
	"iter" : 50000,				// Simulation iterations
	"age" : 50,					// Target signal age to form loop
	"total_act_target" : 0,		// Target for total activity level (set < 1 to disable)
	"branch_window" : 30,		// Time window for allowing branching
	"propagate_noise" : 0.01,	// Noise factor for update success
	"select_noise" : 0.1,		// Noise factor for path selection
	"slow" : 0,					// Number of ms delay per frame
	"write_frames" : 0			// Write last visited data to file
}
